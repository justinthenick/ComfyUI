# Byte-compiled / cache
__pycache__/
*.py[cod]

# Virtual envs / IDE
.venv/
venv/
.vscode/
.idea/
.vs/
.DS_Store

# Local logs
*.log
logs/

# ComfyUI generated / large artifacts (kept external anyway)
/output/
/temp/
/web_custom_versions/
/openapi.yaml
/filtered-openapi.yaml
/uv.lock

# User content (we keep it in E:\ComfyUI_data\user, not in repo)
/user/

# Models are external (E:\ComfyUI_data\models); ignore any stray ones here
/models/

# Custom nodes — ignore everything except the Manager submodule
/custom_nodes/*
!/custom_nodes/comfyui-manager/

# IMPORTANT: Track the external mapping config
# (Remove this line if it exists in your current ignore file)
#/* DO NOT ignore */ extra_model_paths.yaml

ComfyUI/custom_nodes/comfyui-manager/

